# 에이전트 안내

이 저장소는 아이데이션 전용이다. 세션이 시작되면 코드를 만들지 말고, 아래를 먼저 한다.

1. `ideas/YYYY-MM-DD-<slug>/` 폴더와 `README.md`를 연다.
2. `main`에서 `idea/YYYY-MM-DD-<slug>` 브랜치를 딴다. Cursor Cloud가 `cursor/<name>-<id>`를 강제하면 그 브랜치를 유지하고 폴더만 규칙대로 연다.
3. 산출물(md, pdf)은 그 폴더에만 둔다.
4. 루트 `README.md` 카탈로그에 한 줄을 추가하고 커밋한다.

상세는 `.cursor/rules/ideation-git.mdc`를 따른다.
